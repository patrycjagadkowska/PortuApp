import classes from './styles/Conjugation.module.css';

const Conjugation = props => {
    return (
        <div className={classes.conjugation}>
            <h1>This is conjugation</h1>
        </div>
    );
};

export default Conjugation;